# Optional. In dev the Vite proxy forwards /api to the api-server, so the
# default relative base works with no config. Set this only to point at a
# non-proxied API (e.g. a deployed backend).
# VITE_API_BASE_URL=/api/v1
